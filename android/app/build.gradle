plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace "com.lingshu.paipan"
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion
    ndkVersion rootProject.ext.ndkVersion

    defaultConfig {
        applicationId "com.lingshu.paipan"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.2.2"
        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86', 'x86_64'
        }
    }

    signingConfigs {
        release {
            storeFile file('release.keystore')
            storePassword 'Lingshu@123'
            keyAlias 'lingshu-release-key'
            keyPassword 'Lingshu@13'
            v1SigningEnabled true
            v2SigningEnabled true
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation 'com.facebook.react:react-native:0.72.7'
    implementation 'com.swmansion.rnscreens:rnscreens:4.24.0'
    implementation 'com.th3rdwave.safeareacontext:safe-area-context:4.9.0'
    if (project.hasProperty("hermesEnabled") && project.hermesEnabled.toBoolean()) {
        implementation 'com.facebook.react:hermes-android:0.72.7'
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
        maven { url 'https://mirrors.cloud.tencent.com/nexus/repository/maven-public/' }
    }
}