// 已完全移除 pluginManagement，不依赖 node_modules
ext {
    buildToolsVersion = "34.0.0"
    minSdkVersion = 24
    compileSdkVersion = 34
    targetSdkVersion = 34
    ndkVersion = "25.1.8937393"
    kotlinVersion = "1.7.22"  // RN 0.72.7 必须用 1.7.22
}
rootProject.name = 'DivinationApp'

// 不再依赖 node_modules 中的 native_modules.gradle，手动声明 app 模块
println "⚠️ 已移除对 node_modules 的依赖，使用手动 app 模块配置"
include ':app'
