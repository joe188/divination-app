version: '1.0'
name: lingshu-build
displayName: lingshu-build
triggers:
  trigger: auto
  push:
    branches:
      prefix:
        - ''
stages:
  - name: stage-6f69ef59
    displayName: 未命名
    strategy: naturally
    trigger: auto
    executor:
      - joelinfo
    steps:
      - step: build@gradle
        name: build_gradle
        displayName: Gradle 构建
        jdkVersion: '8'
        gradleVersion: '4.4'
        commands:
          - bash gitee-go-build.sh
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./target
        caches:
          - ~/.gradle/caches
        notify: []
        strategy:
          retry: '0'
